import AddPhotoCard from "@/ui/AddPhotoCard";
import { LayoutProps } from "@/ui/types";

const Layout = ({ children }: LayoutProps) => {
  return (
    <>
      {children}
    </>
  );
};

export default Layout;
